<div class="modal hide fade" id="modal-content-edit-activity" data-keyboard="false" data-backdrop="static" data-width="760">
  <div class="modal-header">
    <h3>
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    Edit <%= @actual_phase.name %>  Steps
    </h3>
  </div>
  <div class="modal-body">
    <table class="table table-condensed">
      <tbody>
        <% @user_phase.activities.sort_by(&:activity_date).each do |activity| %>
        <tr>
          <%= simple_form_for activity, :url => activity_path(activity), :html => {:method => :put, :class => 'form-horizontal' } do |f| %>
          <td>
            s
            :input_html => {
            :id => "activity_date_"+activity.id.to_s
            } %>
            
          </td>
          <td>
            <%= f.input :amount, :autofocus => false, :class => 'row-fluid pull-right' %>
          </td>
          <td>
            <%= f.button :submit, "Update", :class => 'btn btn-primary' %>
          </td>
          
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<script type="text/javascript">
$(function() {
<% @user_phase.activities.each do |activity| %>
$( "<%="#activity_date_"+activity.id.to_s%>").datepicker(
{
  startDate: "<%= @user_phase.start_date.strftime("%m/%d/%Y") %>",
  endDate: "<%= @user_phase.end_date.strftime("%m/%d/%Y") %>"
}
);
<% end%>
});
</script>